<%= render "shared/heading" %>
<table>
    <thead>
        <td>Name</td>
        <td>Difficulty</td>
    </thead>
    <tbody>
        <% @recipes.each do |recipe| %>
            <tr id="recipe-<%= recipe.id %>">
                <td><%= recipe.name %></td>
                <td><%= recipe.difficulty %></td>
            </tr>
        <% end %>
    </tbody>
</table>
